# Entorno virtual y cachés Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/

# Archivos sensibles / locales (NO van al APK)
.env
*.log

# app.env SÍ debe empaquetarse (tema, API, cámara). No listar aquí.

# Artefactos de compilación
build/
build_old_*/
_BORRAR_*/
dist/
*.apk
*.aab
*.sha1

# Datos locales de operación (no deben ir al APK)
fotos_asistencia/
offline_storage/
assets/tmp/

# Servidor FastAPI y firmas Android (no son parte de la app móvil)
api.py
requirements-api.txt
*.jks
*.keystore